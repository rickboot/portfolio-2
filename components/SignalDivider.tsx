export default function SignalDivider() {
  return (
    <div
      className="w-full h-px"
      style={{
        background:
          'linear-gradient(90deg, transparent 0%, var(--cyan-dim) 20%, var(--cyan-dim) 80%, transparent 100%)',
      }}
    />
  )
}
